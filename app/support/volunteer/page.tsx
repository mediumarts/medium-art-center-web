import ComingSoon from "@/components/ComingSoon";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Become a Volunteer" };

export default function Page() {
  return <ComingSoon title="Become a Volunteer" />;
}
