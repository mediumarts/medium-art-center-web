import ComingSoon from "@/components/ComingSoon";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Show Schedule" };

export default function Page() {
  return <ComingSoon title="Show Schedule" />;
}
